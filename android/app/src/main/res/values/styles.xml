<?xml version="1.0" encoding="utf-8"?>
<!-- Theme.MaterialComponents.*, not @android:style/Theme.* — Stripe's
     PaymentSheet (カード人質, docs/BILLING_API.md) inflates AppCompat/Material
     views and crashes under a plain platform theme. NoActionBar keeps the
     window exactly as bare as NoTitleBar did. -->
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <!-- Dark even in the light theme: see res/values/colors.xml. -->
        <item name="android:windowBackground">@color/launchBackground</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
