{
  "name": "wake-or-pay-discord-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Discord OAuth2 code exchange for Wake or Pay. Holds the client secret so the APK does not have to.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0",
    "@cloudflare/workers-types": "^4.20250109.0"
  }
}
